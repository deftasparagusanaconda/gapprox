the main reason of representing expressions as a DAG is to uncover any underlying structure as much as possible. that is why nodes are subtyped into InputNode, FunctionNode, OutputNode, and why Symbols are subtyped into Variable, Parameter, Constant. there is no implied behaviour in terms of representation. everything is very clear in what its supposed to do and what its supposed to represent
